'use client'

import { HEADER_SIDEBAR_SIZE } from '@/lib'

export const Header = () => {
  return (
    <div className={HEADER_SIDEBAR_SIZE.H}>
      <p className='p-2'>AppLayout</p>
    </div>
  )
}
